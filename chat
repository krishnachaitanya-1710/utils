All nodes are using the same service account, so whenever a specific node tries to pull the artifact, it has a global view from the dtcc-maven-public repository. This is causing a conflict with artifacts. I will explain further when we touch base.”
